# Методика оценки российского бизнеса для M&A
### Инструкция для агента-оценщика · рынок РФ 2025–2026 · версия 06.2026

> **Кто это читает.** Ты — агент, который получает данные о российской компании
> (из `data_fetcher.py`: `company_info`, `financials`, `risk_flags`, `macro`) и должен
> выдать **обоснованную оценку стоимости** бизнеса и/или доли. Этот документ —
> самодостаточный алгоритм: иди по **дереву решений** до конкретной формулы,
> применяй корректировки, согласуй методы, выдай диапазон.
>
> **Числа** (мультипликаторы, ставки, дисконты) лежат в `valuation_params.yaml` —
> читай их оттуда, не зашивай в голову. Здесь — логика и формулы.
>
> **Главное правило честности.** Если данных мало (`data_quality < 0.4`) —
> оценка индикативная, прямо это укажи. Не выдавай точечное число там, где
> уместен только диапазон. Всегда показывай диапазон (low / base / high).

---

## 0. Как пользоваться (порядок действий)

1. **Собери вход** → раздел 1.
2. **Нормализуй** финансы (единицы, EBITDA, серая выручка, ИП+ООО) → раздел 2.
3. **Пройди дерево решений** → раздел 3. На выходе — основной метод(ы).
4. **Посчитай стоимость** выбранным методом → раздел 4 (формулы).
5. **Ставка дисконтирования** (если DCF) → раздел 5.
6. **Корректировки** (дисконты/премии, мост EV→Equity) → раздел 6.
7. **Учти риск-флаги** → раздел 7 (могут быть deal-breaker'ы).
8. **Согласуй методы** в итоговый диапазон → раздел 8.
9. **Проверь deal-breakers и выдай результат** → разделы 9–10.

---

## 1. Входные данные

Минимум для содержательной оценки:

| Данные | Откуда | Если нет |
|---|---|---|
| Выручка, прибыль от продаж (EBIT), чистая прибыль за 3–5 лет | `financials.years[*]` (GIR БО / OFData) | оценка по активам или индикативная |
| Активы, собственный капитал, долг, денежные средства | `financials.years[*]` | мост EV→Equity невозможен — оговорить |
| Отрасль (ОКВЭД) | `company_info.okved_code` | `multiples.default` |
| Размер (выручка), форма (ООО/АО/ИП), МСП | `company_info` | классифицировать по выручке |
| Статус, банкротство, суды, ФССП, налоги, госконтракты | `risk_flags` | повышенная неопределённость |
| Ключевая ставка, ОФЗ | `macro` | fallback из `valuation_params.yaml` |

**Контекст сделки** (если известен) — спроси/учти: оценивается **100%** или **доля**?
**контрольный** пакет или **миноритарный**? продавец — **резидент** или **иностранец на выходе**?
Это меняет корректировки (раздел 6).

---

## 2. Нормализация (до расчётов)

1. **Единицы.** ГИР БО отдаёт суммы в **тысячах рублей**. Приведи всё к одной
   размерности (рекомендуется — рубли) и зафиксируй.
2. **EBITDA, если её нет.** ГИР БО не даёт амортизацию напрямую. Тогда:
   `EBITDA = EBIT(стр.2200) + амортизация`, где амортизацию оцени как
   `da_as_pct_of_revenue` по капиталоёмкости отрасли (`valuation_params → ebitda_proxy`).
   Если совсем ничего — грубо `EBITDA ≈ EBIT × 1.15`. **Помечай как оценочную.**
3. **Нормализация прибыли (важно для СМБ).** Убери из прибыли разовое и
   «личное» собственника: разовые сделки, нерыночную зарплату/аренду от связанных
   лиц, личные расходы через фирму. Для микробизнеса считай **SDE**
   (Seller's Discretionary Earnings) = чистая прибыль + зарплата владельца +
   амортизация + разовые/личные расходы.
4. **Серая выручка.** Если часть оборота неучтённая (типично для СМБ-кэша):
   учитывай её прибыль с дисконтом доверия `gray_revenue_haircut` (учесть 30–70%,
   остальное срезать). Никогда не бери серую выручку «как есть» — покупатель
   платит за то, что подтверждаемо и переносимо.
5. **ИП + ООО (частая структура).** Реальный бизнес часто размазан между ИП
   (низкий налог, кэш) и ООО (контракты, активы). **Консолидируй**: сложи выручку/
   прибыль/активы по всем связанным лицам (см. `risk_flags.affiliated`), убери
   внутренние обороты. Оценивай экономический бизнес, а не одно юрлицо.
6. **Налоговый режим.** ОСНО (налог на прибыль 25% с 2025) vs УСН (6% / 15%).
   При нормализации к посленалоговой прибыли используй фактический режим.

---

## 3. ДЕРЕВО РЕШЕНИЙ

Иди сверху вниз. Первое сработавшее условие определяет ветвь. Узлы — `N*`.
В скобках — выход на метод(ы) из раздела 4.

```
N0. СТАРТ: достаточно ли данных?
 ├─ data_quality(financials) < 0.4 И нет баланса
 │     → N0a. ИНДИКАТИВНАЯ оценка: только мультипликатор по выручке (EV/Sales)
 │            или по аналогам/rule of thumb. Явно пометить низкую достоверность.
 └─ иначе → N1

N1. ЖИВ ЛИ БИЗНЕС? (going concern)
 ├─ status == BANKRUPT / конкурсное производство
 │     → [M-LIQ] ЛИКВИДАЦИОННАЯ стоимость (активы − обязательства − дисконт). STOP.
 ├─ status == LIQUIDATING (ликвидация)
 │     → [M-NAV/M-LIQ] стоимость чистых активов с дисконтом. STOP.
 ├─ собственный капитал < 0  ≥2 лет  И  убытки ≥3 лет (см. thresholds.distress)
 │     → [M-LIQ] distressed: ликвидационная/чистые активы. STOP.
 └─ иначе → N2

N2. ТИП БИЗНЕСА ПО ПРИРОДЕ СТОИМОСТИ
 ├─ ОКВЭД == 68 (недвижимость/аренда)
 │     → [M-CAP] доходная капитализация NOI / cap rate (раздел 4.4). → затем N6.
 ├─ ОКВЭД ∈ {64,65,66} (банки/МФО/лизинг/финансы)
 │     → [M-PB] оценка по капиталу (P/B) и P/E (раздел 4.5). → затем N6.
 ├─ холдинг/инвесткомпания (активы ≫ операц. прибыль; выручки почти нет,
 │   на балансе фин.вложения/доли)
 │     → [M-NAV] стоимость чистых активов (рыночная переоценка). → затем N6.
 └─ операционный бизнес → N3

N3. ПРИБЫЛЬНОСТЬ И ДИНАМИКА (по нормализованным данным)
 ├─ стабильно прибыльный (EBITDA>0 ≥2–3 лет, выручка не падает резко)
 │     → N4 (полноценная оценка: доходный + сравнительный)
 ├─ убыточный, НО быстрый рост выручки (IT/маркетплейс/EdTech, рост >30%/год)
 │     → [M-SALES] оценка по выручке (EV/Sales, ARR) + [M-DCF] с фазой роста.
 │            → затем N6.
 ├─ прибыль волатильна / разовые всплески
 │     → нормализуй (усредни 3 года, убери разовое) → N4, но вес мультипликатора выше DCF
 └─ убыточный без роста, операционно слабый
 │     → [M-NAV/M-LIQ] активы как «пол» стоимости + опцион на оздоровление. → N6.

N4. РАЗМЕР И НАДЁЖНОСТЬ ОТЧЁТНОСТИ → выбор связки методов
 ├─ микро/малый (выручка < 800 млн ₽) ИЛИ сильная завязка на собственника
 │     ОСНОВНОЙ:  [M-MULT] мультипликатор (EV/EBITDA или P/E) по отрасли
 │     ПЕРЕКРЁСТНО: [M-SDE] rule of thumb (мес. прибыли / окупаемость)
 │     DCF — опционально (если есть вменяемый прогноз). → N5/N6.
 ├─ средний (0.8–2 млрд ₽), отчётность вменяемая
 │     ОСНОВНОЙ:  [M-DCF] (FCFF) + [M-MULT] EV/EBITDA как кросс-чек. → N5/N6.
 └─ крупный (>2 млрд ₽), есть МСФО/аудит
 │     ОСНОВНОЙ:  [M-DCF] (FCFF), [M-MULT] по торгуемым аналогам Мосбиржи. → N5/N6.

N5. (если выбран DCF) → построй ставку дисконтирования (раздел 5), посчитай FCFF.

N6. КОРРЕКТИРОВКИ → раздел 6 (DLOM, контроль/миноритарий, ключевой человек,
    концентрация, серая выручка, санкции, господдержка, мост EV→Equity).

N7. РИСК-ФЛАГИ → раздел 7 (доп. дисконт или deal-breaker).

N8. СОГЛАСОВАНИЕ методов → итоговый диапазон (раздел 8). Выдай результат.
```

**Карта методов:** `M-DCF` доходный (DCF/FCFF) · `M-MULT` сравнительный
(EV/EBITDA, P/E) · `M-SALES` по выручке (EV/Sales, ARR) · `M-SDE` rule of thumb /
SDE · `M-CAP` капитализация NOI (недвижимость) · `M-NAV` чистые активы ·
`M-LIQ` ликвидационная · `M-PB` по капиталу (финкомпании).

---

## 4. Формулы методов

### 4.1 [M-MULT] Сравнительный (мультипликаторы)
1. Определи отрасль по ОКВЭД → возьми диапазон из `valuation_params.multiples`.
2. Выбери базу: устойчивый прибыльный бизнес → **EV/EBITDA**; для СМБ часто
   удобнее **P/E** (мультипликатор чистой прибыли).
3. Возьми **нормализованную** EBITDA/прибыль (среднюю за 2–3 года или LTM, если
   динамика ровная; при росте — последний год).
4. Выбор точки в диапазоне `[low, base, high]` — по позиционированию:
   - **выше base**: рост выручки >15%/год, маржа выше отраслевой, диверсификация
     клиентов, узнаваемый бренд, повторяемая выручка, низкий долг;
   - **ниже base**: падение/стагнация, зависимость от 1 клиента/собственника,
     высокий долг, слабый учёт, регуляторные/санкционные риски.
5. `EV = мультипликатор × EBITDA`  (или `Equity = P/E × Чистая прибыль`).
6. Если считал через EV → перейди к мосту EV→Equity (раздел 6).

### 4.2 [M-DCF] Доходный (FCFF, дисконтированные денежные потоки)
```
FCFF_t = EBIT_t × (1 − tax) + амортизация_t − CAPEX_t − ΔОборотный_капитал_t
EV = Σ FCFF_t / (1+WACC)^t  +  TV / (1+WACC)^n
```
- Горизонт `n = dcf.horizon_years` (5 лет). Прогноз выручки — от истории и отрасли,
  консервативно (высокая ставка ЦБ давит спрос).
- `tax = 0.25` (ОСНО, с 2025) либо ставка УСН.
- **Терминальная стоимость (TV):**
  - Гордон: `TV = FCFF_{n}×(1+g) / (WACC − g)`, `g = terminal_growth_g` (≤ инфл. таргет 4%);
  - либо exit-multiple: `TV = EBITDA_n × exit_multiple_ev_ebitda`.
- `WACC` — раздел 5. EV получен — иди на мост EV→Equity (раздел 6).
- **Санити:** если `WACC` вне `[min_wacc, max_wacc]` — перепроверь входные ставки.

### 4.3 [M-SALES] По выручке (для растущих/убыточных)
`EV = EV/Sales × Выручка` (LTM). Для SaaS: `EV = (2–5) × ARR`.
Применяй, когда прибыли нет/она не репрезентативна, но есть рост и масштаб.
Обязательно проверяй «дорогу к прибыли» (unit-экономику) — иначе верхнюю границу не бери.

### 4.4 [M-CAP] Капитализация дохода (недвижимость/аренда)
```
NOI = чистый операционный доход (аренда − операц. расходы, без долга и налога)
Стоимость = NOI / cap_rate
```
`cap_rate` для РФ-2026 — `multiples.real_estate.cap_rate` (10–14%). Чем выше ставка
ЦБ и риск локации — тем выше cap_rate и ниже стоимость. Сверь с затратным
(рыночная стоимость объектов − обязательства).

### 4.5 [M-PB] Финансовые компании
`Equity = P/B × Собственный капитал`, кросс-чек `P/E × Чистая прибыль`.
EV/EBITDA **не применять**. Проверь лицензию ЦБ (отзыв = deal-breaker).

### 4.6 [M-NAV] Чистые активы
`Equity = Рыночная стоимость активов − Обязательства`. Переоцени ключевые активы
к рынку (недвижимость, оборудование, запасы — с дисконтом на ликвидность;
дебиторку — за вычетом безнадёжной; НМА/гудвилл — осторожно). Это «пол» для
операционного бизнеса и основной метод для холдингов.

### 4.7 [M-LIQ] Ликвидационная
`= NAV − дисконт быстрой реализации (обычно 20–50% к активам) − издержки ликвидации
− приоритетные требования (зарплаты, налоги, залоги)`. Для банкрота/ликвидации.

### 4.8 [M-SDE] Rule of thumb (микробизнес)
Цена ≈ **месячная чистая прибыль × K мес.**, где K (окупаемость):
- кафе/розница/услуги с высокой завязкой на собственника: **8–18 мес** (0.7–1.5 года);
- стабильный сервис с управляющим и повторяемой выручкой: **24–36 мес** (2–3 года).
Эквивалент `P/E ≈ K/12`. Использовать как перекрёстную проверку, не как единственный метод.

---

## 5. Ставка дисконтирования (build-up / CAPM), РФ-2026

Считаем **в рублях**. Безрисковая — **доходность ОФЗ**, она уже включает рублёвую
инфляцию и страновой риск → **отдельную страновую премию (CRP) НЕ добавляем**
(иначе двойной счёт; CRP нужен только если считать от долларовой базы).

**Стоимость собственного капитала (Ke):**
```
Ke = Rf + β × ERP + Size_premium + Specific_risk
```
- `Rf` = `macro.ofz_10y` (≈ 13.5% на 06.2026; бери живое из `get_macro()`).
- `β` = отраслевая beta (`multiples.<отрасль>.beta`). Для непубличных можно brать
  как есть (proxy). При желании релевередж: `β_L = β_U × (1 + (1−tax)×D/E)`.
- `ERP` = `macro.equity_risk_premium` (≈ 8%).
- `Size_premium` = `risk_premiums.size_premium[размер]` (микро +6 … крупн. 0 п.п.).
- `Specific_risk` = сумма надбавок `risk_premiums.specific_risk` (ключевой человек,
  концентрация, серая выручка, санкции, слабое управление), кап 8 п.п.

**Стоимость долга (Kd):**
```
Kd_after_tax = (key_rate + spread[размер]) × (1 − tax)
```
`key_rate` = `macro.key_rate` (≈ 14.5%); `spread` = `cost_of_debt.spread_by_size`.

**WACC:**
```
WACC = Ke × E/(D+E) + Kd_after_tax × D/(D+E)
```
`D`, `E` — по рыночной (или балансовой как proxy) структуре. Если долг ≈ 0 → WACC ≈ Ke.

> **Пример порядка величин (06.2026, малый бизнес, услуги):**
> Ke = 13.5 + 1.0×8 + 4(small) + 3(ключевой человек) = **28.5%**.
> Это нормально для РФ-2026 — высокая ставка ЦБ делает деньги дорогими, и
> мультипликаторы это уже отражают (низкие 3–5x). Не занижай ставку искусственно.

---

## 6. Корректировки (порядок применения)

Применяй к стоимости **100% собственного капитала**, последовательно (мультипликативно).

1. **Мост EV → Equity** (если метод дал EV: DCF, EV/EBITDA, EV/Sales):
   `Equity = EV − Чистый долг`, где `Чистый долг = Долг − Денежные средства`
   (`financials.years[latest].net_debt`). Плюс/минус неоперационные активы/обязательства.
2. **DLOM** (неликвидность частной компании): `× (1 − dlom)`, типично 0.25.
   Не применять, если уже использовал мультипликаторы по **частным** сделкам
   (там неликвидность уже внутри) — применяй DLOM в основном к DCF и к
   мультипликаторам публичных аналогов.
3. **Контроль / доля:**
   - оцениваешь **миноритарную** долю → `× (1 − minority_discount)` (≈0.25);
   - **контрольный** пакет от базы миноритария → `× (1 + control_premium)` (≈0.20);
   - доля = `Equity × %доли` уже после этих коэффициентов.
4. **Ключевой человек:** `× (1 − key_person)` (0.10–0.30), если бизнес держится на собственнике.
5. **Концентрация клиентов:** `× (1 − customer_concentration)`.
6. **Серая выручка:** уже учтена в нормализации (раздел 2.4); не дисконтируй дважды.
7. **Санкции:** `× (1 − sanction_discount)` по уровню экспозиции отрасли/компании.
8. **Иностранный продавец на выходе:** `× (1 − foreign_seller_discount)` (0.50–0.70) —
   **УСЛОВНО** (калибровка по backtest, см. `backtest_report.md`). Применять ТОЛЬКО к
   вынужденным/«пожарным» продажам без конкуренции покупателей. НЕ применять к
   конкурентным стратегическим активам: пример — Avito (выход Prosus, 2022) был продан
   по полному IT-мультипликатору (низ диапазона) без дисконта, т.к. был спрос и
   банковское финансирование. Критерий: есть конкуренция за актив → дисконт ≈ 0;
   единственный вынужденный выход под давлением → 0.50–0.70.
9. **Господдержка/субсидии/устойчивые госконтракты:** `× (1 + state_support_premium)`.

> Не складывай бесконечно дисконты — это даёт абсурдно низкую цену. Применяй
> только релевантные, держи суммарный эффект осмысленным и поясняй каждый.

---

## 7. Учёт риск-флагов (`risk_flags`)

| Флаг | Действие |
|---|---|
| `BANKRUPT` / конкурсное производство | **deal-breaker** → ветвь M-LIQ, STOP |
| `LIQUIDATING` | M-NAV/M-LIQ; для going-concern не оцениваем |
| Блокировка счетов (active) | критично: операционный паралич → высокий дисконт/стоп |
| ФССП: крупные исполнительные производства | вычесть из стоимости как обязательство + риск |
| Налоговая задолженность | вычесть сумму; систематическая → дисконт за управление |
| Иски как **ответчик** на крупные суммы | вычесть ожидаемые потери (сумма × вероятность) |
| Иски как **истец** | условный плюс (взыскания), но дисконтировать на взыскаемость |
| Лицензия отозвана / нет обязательной лицензии (ЦБ, мед, СРО) | deal-breaker для лицензируемых видов |
| В реестре недобросовестных поставщиков (РНП) | дисконт, ограничение на госзаказ |
| Много аффилированных лиц в плохом состоянии | риск вывода активов / субсидиарки → дисконт |
| `risk_score` высокий | повышай долю консервативных методов (NAV) и нижнюю границу диапазона |

Денежные обязательства (ФССП, налоги, ожидаемые проигрыши) вычитай из Equity
**в мосте** (раздел 6.1), не дублируя в дисконтах.

---

## 8. Согласование методов и итог

1. Посчитай 2–3 метода из выбранной связки (например, DCF + EV/EBITDA + SDE-чек).
2. Если результаты расходятся **<30%** — взвесь:
   - устойчивый средний/крупный бизнес: DCF 50–60%, мультипликатор 40–50%;
   - СМБ/завязка на собственника: мультипликатор 50–60%, SDE 20–30%, DCF ≤20%;
   - активо-ёмкий/холдинг: NAV 50%+, доходный — остаток.
3. Если расходятся **>30%** — не усредняй слепо: найди причину (нерепрезентативная
   прибыль? агрессивный прогноз? недооценённые активы?), выбери надёжный метод
   как опорный, остальные — как границы.
4. **Выдай диапазон** `low / base / high`, а не одно число. `base` — взвешенная
   оценка; `low`/`high` — из нижних/верхних мультипликаторов и сценариев DCF.
5. Укажи: метод(ы), ключевые допущения, `data_quality`, главные риски, что
   проверить в due diligence.

**Формат результата (рекомендуемый):**
```
Компания: <название>, ИНН <inn>, отрасль <ОКВЭД/название>
Стоимость 100% (Equity): low … base … high млн ₽
Оцениваемая доля: <%> → … млн ₽
Метод(ы) и веса: ...
Ставка дисконтирования (если DCF): WACC = ...%
Ключевые корректировки: ...
Риск-флаги: ...
Достоверность данных: data_quality = ...
Оговорки: ...
```

---

## 9. Чек-лист deal-breakers (проверь до выдачи)

- [ ] Банкротство / конкурсное производство.
- [ ] Отзыв/отсутствие обязательной лицензии (ЦБ, мед, образование, СРО).
- [ ] Отрицательный капитал + хронические убытки (going concern под вопросом).
- [ ] Активная блокировка счетов / арест ключевых активов.
- [ ] Налоговый долг/иски, сопоставимые со стоимостью бизнеса.
- [ ] Стоимость держится только на серой выручке (непереносима покупателю).
- [ ] Бизнес = один человек без переносимых активов/контрактов.

Любой пункт → либо стоп, либо ветвь M-LIQ/M-NAV, либо явная крупная скидка с пояснением.

---

## 10. Ключевые принципы (помни всегда)

1. **Покупатель платит за переносимое и подтверждаемое.** Серая выручка, личные
   связи собственника, незадокументированное — со скидкой или мимо кассы.
2. **Высокая ставка ЦБ (14.5%) — это уже в цене.** Поэтому мультипликаторы РФ-2026
   низкие (медиана EV/EBITDA ~5x), а ставки дисконтирования высокие (Ke 20–30%).
   Не «чини» это занижением ставок или завышением мультипликаторов.
3. **Диапазон, а не точка.** Особенно при `data_quality < 0.6`.
4. **Активы — пол, доход — потолок.** Operating business стоит не меньше чистых
   активов и не больше дисконтированных денежных потоков (в норме).
5. **Объясняй каждый дисконт/премию.** Непрозрачная корректировка = ошибка.

---

## Источники (для калибровки чисел)

- **Мультипликаторы сделок РФ 2024–2025:** AK&M, Advance Capital (медиана
  EV/LTM EBITDA ~4.8x, локальные ~5.1x, иностранные «на выходе» ~2.0x, дисконт 60%+);
  обзоры РБК; IPO IT H1-2024 — 9–11x. KPMG/Kept/Б1 M&A reviews, Cbonds, Мосбиржа.
- **СМБ rule of thumb:** Авито Бизнз, Альтера Инвест, «Нескучные финансы» —
  2–5× чистой прибыли / окупаемость 1–3 года.
- **Макро (06.2026):** ЦБ РФ — ключевая ставка 14.5% (с 24.04.2026); ОФЗ-10л ~13.5%;
  инфляция ~5.8%; ERP РФ ~8–11% (Дамодаран, страновые премии).
- **Налоги:** налог на прибыль 25% с 01.01.2025.

> Числа — рыночные ориентиры на дату, не нормативы. Перед сделкой сверяй с
> свежими обзорами и реальными аналогами. Обновляй `valuation_params.yaml`.

---
---

# ЧАСТЬ II. РАСШИРЕННЫЕ МОДУЛИ (Фаза 0) — приближение к реальности

> Часть I даёт стоимость по официальным трейлинг-данным = **нижняя граница**.
> Модули ниже добавляют то, чего нет в РСБУ: истинный масштаб (серая выручка),
> полный периметр (группа), стоимость будущего (форвард) и нематериальные активы.
> Применяй их ПОСЛЕ базовой оценки, чтобы получить **справедливый диапазон**.

## 11. Предобработка-2: периметр и истинный масштаб (ДО оценки)

Выполни перед разделом 4, дополняя нормализацию (раздел 2):

**11.1. Консолидация группы.** Вызови `get_affiliates(inn)`:
- `owners` — учредители (с долями) и руководитель, их ИНН;
- `predecessors` — правопредшественники (удлиняют трек-рекорд, тяни их финансы);
- `related_companies` — другие компании учредителей (если получены).
- **Проверь, нет ли у собственников ИП/второго ООО** (по ИНН учредителя-ФЛ через
  `get_company_info(инн_учредителя)` — для 12-значных сработает ИП-ветка). Частая
  схема: ИП на собственнике держит активы/кэш/льготный налог. Если нашёл —
  **консолидируй**: сложи выручку/активы группы, убери внутренние обороты.
  *(Пример: у ООО «Кармин-Авто» собственник Мухин А.В. — сам ИП с ОКВЭД 77.39.2
  «аренда оборудования»: вероятно, термопластавтоматы на ИП сдаются в ООО.)*

**11.2. Триангуляция истинной выручки (серая).** Вызови `get_scale_signals(inn)`:
- сравнивает официальную выручку с implied = `сотрудники × выручка_на_сотрудника`
  (бенчмарк из `valuation_benchmarks.yaml`);
- `gray_flag`: `none` / `moderate` / `strong`.
- **Если флаг moderate/strong** — официальные данные занижены, добавь сигналы:
  - **маркетплейс** (Moneyplace/MPStats): продажи бренда на WB/Ozon в ₽;
  - **импорт** (таможня): объём ввоза × наценка (для дистрибьюторов);
  - **число клиентов × средний чек** (из деки/DD);
  - **мощность × загрузка** (оборудование/площади).
- `implied_true_revenue` = оценка реального оборота (max/блендинг сигналов).
- **Оговорка:** высокая выручка/сотрудник в дистрибуции/опте — норма, не серое.
  Решение принимай по нескольким сигналам, а не по одному.

**11.3. Учёт серого в стоимости.** Серая прибыль — реальна для текущего владельца,
но **непереносима** покупателю (нельзя задекларировать, нет залога, налоговый риск):
1. Оцени истинную EBITDA (вкл. серую часть).
2. К серой части примени `transferable_share_of_gray` (0.30–0.70) — платят только за
   переносимое.
3. Добавь риск-надбавку/индемнити (доначисления ФНС).
> Это объясняет, почему официальная оценка низкая, а «цена в голове» собственника
> высокая: он знает истинную экономику. Реалистичная оценка — между ними.

**11.4. Плаузибилити-проверка (для ЛЮБОГО бизнеса).** Сверь с бенчмарками:
EBITDA-маржа, выручка/сотрудник, оборачиваемость, средняя зарплата. Аномалии →
либо ошибка данных, либо серое, либо особенность (поясни). Это и есть способ
**обобщать на массу бизнесов**: бенчмарк нормализует разнородные компании.

## 12. ИП и микробизнес без отчётности

У **ИП нет бухотчётности в ГИР БО вообще** (12-значный ИНН → `is_ip=true`). Также
многие микро-ООО на УСН подают пустой/минимальный РСБУ. Алгоритм:
1. Оцени выручку по **бенчмаркам и косвенным сигналам** (раздел 11.2): сотрудники,
   точки/площади, маркетплейс, патент/режим налогообложения, число клиентов.
2. Оцени прибыль через отраслевую **EBITDA-маржу** из `valuation_benchmarks.yaml`.
3. Метод — **SDE / rule of thumb** (раздел 4.8): 8–24 месячных прибыли.
4. data_quality будет низким →只 диапазон, явная оговорка «оценка по косвенным данным».

## 13. Форвард-модуль: оценка роста и катализаторов

Применяй на ветви **N3 «убыточный, но быстрый рост»** и для зрелых компаний с
**бизнес-планом/катализатором** (новые продукты, СТ-1, экспорт, M&A синергии).
Трейлинг-методы такие кейсы занижают.

**13.1. Сценарный DCF.** Построй 3 сценария на 5 лет (выручка→EBITDA→FCFF):
- **Медведь** — план не реализован, возврат к текущему уровню;
- **База** — умеренная реализация;
- **Бык** — план выполнен.
Дисконтируй каждый по WACC (раздел 5) → EV сценария. Затем взвесь по вероятности:
`EV_ожид = Σ P(сценарий) × EV(сценарий)`, где `Σ P = 1`.

**13.2. Дерево бинарного катализатора** (событие «да/нет», напр. лицензия/сертификат/
крупный контракт):
```
        ┌─ катализатор СРАБОТАЛ (p)   → EV_высокий (открывается рынок/канал)
EV_ожид ┤
        └─ НЕ сработал (1−p)          → EV_базовый
EV_ожид = p × EV_высокий + (1−p) × EV_базовый
```
`p` оценивай по фактам: статус заявки, наличие LOI/предконтрактов, история компании.
**Не верь плану на 100%** (p<1) и не игнорируй его (p>0).

**13.3. Дисконт на исполнение.** Прогнозные потоки умножай на коэффициент доверия
к плану (0.5–0.9) в зависимости от того, подтверждён ли он фактами и треком команды.

## 14. Гудвилл и нематериальные активы (то, чего нет в РСБУ)

Для бизнесов с брендом/дистрибуцией/технологией добавляй к доходной/активной оценке:
- **Бренд / товарные знаки** — relief-from-royalty: `стоимость = Σ дисконт.(роялти% ×
  выручка)`, роялти 1–5% по силе бренда. Проверь знаки в Роспатент/ФИПС.
- **Дистрибьюторские/эксклюзивные контракты** — excess earnings: часть прибыли,
  обусловленная именно эксклюзивом, капитализированная.
- **Технология / пресс-формы / ПО / патенты** — восстановительная стоимость
  (сколько стоит воссоздать) или relief-from-royalty.
- **Клиентская база** — стоимость привлечения × число активных клиентов × удержание.
> Гудвилл — это разрыв между доходной стоимостью и чистыми активами. Если доходная
> стоимость > NAV, разница и есть нематериальная составляющая; явно её назови.

## 15. Структурирование сделки при разрыве оценок (earn-out)

Когда разрыв «продавец vs покупатель» держится на **нереализованном** будущем
(рост/катализатор/серое, которое надо обелить), не торгуйся об одной цифре — выдай
**структуру**:
- **Аванс сейчас** ≈ финансовая (трейлинг) стоимость — за то, что доказано.
- **Earn-out** (отложенные платежи 1–2 года) ≈ форвард-надбавка — выплачивается при
  достижении фактов: выручка/EBITDA плана, получение лицензии/сертификата,
  подписание ключевых контрактов, обеление оборота.
Это делит риск, выравнивает стимулы, и наш вывод становится не числом, а
**сделочной конструкцией** «X сейчас + до Y под условия».

## 16. Обновлённый порядок действий (с модулями Фазы 0)

```
1. fetch_all(inn)  → company_info, financials, risk_flags, affiliates,
                     scale_signals, benchmark, macro
2. Периметр (11.1): консолидируй группу/ИП собственников/предшественников
3. Истинный масштаб (11.2–11.4): триангуляция серого, плаузибилити по бенчмаркам
4. Дерево решений (раздел 3) → базовая стоимость (Часть I)
5. Если рост/катализатор/бренд → форвард-модуль (13) + гудвилл (14)
6. Корректировки (6) + риск-флаги (7) + учёт серого (11.3)
7. Согласование (8): официальный пол → истинная экономика → переносимо покупателю
8. Если разрыв на будущем → предложи earn-out (15)
9. Выдай ДИАПАЗОН + структуру + data_quality + что проверить в DD
```
